// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom'
import '@testing-library/jest-dom/extend-expect' // so tests can use an extended suit of jest matchers
import 'jest-axe/extend-expect' // so tests user axe-core api to test accessibility
// import { toHaveNoViolations } from 'jest-axe' // so tests user axe-core api to test accessibility
// expect.extend(toHaveNoViolations)
